#include "renderer/rm_lines_stroker/templates/template_functions.h"

void Blank(rMPenFill *fill, Renderer *renderer) {
    // This function is intentionally left blank.
    // Because the blank template contains nothing and needs no implementation.
}
